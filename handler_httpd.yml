---  # My handler playbook
- hosts : webservers
  user  : ansible
  become : yes
  connection : ssh
  tasks :
    - name : Install httpd
      action : yum name=httpd state=present
      notify : start httpd service
  handlers :
    - name : start httpd service
      action : service name=httpd start=started
